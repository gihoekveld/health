info:
  name: Create Test Result
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/tests"
  body:
    type: json
    data: |-
      {
        "date": "2026-03-10",
        "testResults": [
          { "metricId": 1, "value": 134.23}
        ] 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
