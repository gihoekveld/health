info:
  name: Delete Test Result
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/tests/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
