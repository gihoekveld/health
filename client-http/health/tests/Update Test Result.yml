info:
  name: Update Test Result
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/tests/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "date": "2026-03-09",
        "tests": [
          { "metricId": 4, "value": 195.01},
          { "metricId": 2, "value": 20.22}
        ] 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
