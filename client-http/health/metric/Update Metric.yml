info:
  name: Update Metric
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/metrics/:id"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Hemoglobina",
        "unit": "g/dL"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
