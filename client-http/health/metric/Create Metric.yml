info:
  name: Create Metric
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/metrics"
  body:
    type: json
    data: |-
      {
        "name": "Pressão Arterial",
        "unit": "mmHg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
