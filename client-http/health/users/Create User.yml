info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/users"
  body:
    type: json
    data: |-
      {
        "name": "Giselle Hoekveld",
        "email": "gihoekveld@gmail.com",
        "password": "123@Mudar"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
